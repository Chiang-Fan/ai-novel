[<title="NovelCraft 前端需求与演进路线（V1 + V2）">]

好的！以下是 **NovelCraft 系统的前端需求补充**，聚焦于 **V1（MVP）** 和 **V2（伏笔增强）** 两个阶段。设计原则为：**简洁、专注创作、降低认知负荷**，避免花哨干扰。

---

## 一、整体前端架构

- **技术栈建议**：
   - 框架：React（或 Vue 3）
   - 状态管理：Zustand / Pinia（轻量）
   - UI 库：Tailwind CSS + Headless UI（高度可定制）
   - 富文本：Tiptap（支持 Markdown + 自定义节点）
   - 文件处理：`react-dropzone` + `mammoth`（DOCX 解析）

- **核心页面结构**：
  ```
  /dashboard          → 小说列表
  /novel/:id          → 小说工作台（主界面）
    ├─ /outline       → 大纲编辑（V2+）
    ├─ /characters    → 角色面板（V1）
    ├─ /plot-hooks    → 伏笔管理（V2）
    └─ /chapter/:n    → 章节编辑器（核心）
  ```

---

## 二、V1 前端功能需求（MVP）

### 1. 小说仪表盘（/dashboard）
- 显示小说卡片：标题 + 最后编辑时间 + 章节数
- 操作：
   - “新建小说” 按钮
   - “导入小说” 按钮（弹出文件上传模态框）
   - 点击卡片进入 `/novel/:id`

### 2. 小说工作台（/novel/:id）
#### 左侧边栏（固定）
- **基本信息**：标题（可编辑）、章节数
- **导航菜单**：
   - 📖 章节列表（按编号展开/折叠）
   - 👤 角色（V1 只读展示）
   - ⚙️ 风格配置（V1 仅查看，不可编辑）

#### 主内容区（动态）
- 默认显示 **最新章节** 的编辑器
- 支持切换任意章节（点击左侧列表）

### 3. 章节编辑器（核心组件）
- **顶部工具栏**：
   - 章节标题输入框
   - “AI 续写下一章” 按钮（调用 `/chapters?chapterNumber=N+1`）
   - “重写本章” 按钮（保留上下文，重新生成当前章）
- **正文区域**：
   - 富文本编辑器（支持基本格式：粗体、斜体、段落）
   - **关键交互**：
      - 用户可直接修改 AI 生成内容
      - 修改后自动保存（防丢）
      - 显示“AI 生成”徽章（灰色小标签）
- **底部状态条**：
   - 当前字数
   - 节奏提示：“当前处于铺垫阶段” / “已连续 2 章高张力”

### 4. 角色面板（/characters）
- 表格形式展示：
  | 姓名 | 核心信念 | 当前阶段 | 最后出现 |
  |------|--------|--------|--------|
  | 林默 | “信任是软弱” | 受挫期 | 第 5 章 |
- **只读**（V1 不支持手动编辑角色）

### 5. 风格配置面板（/style）
- 以 **只读卡片** 展示系统提取的风格：
  ```text
  • 平均句长：28 字  
  • 高频动词：凝视、颤抖、低语  
  • 禁用词：突然、非常  
  • 描写：高细节，心理以行为暗示为主  
  • 感官侧重：视觉 (70%)，听觉 (20%)
  ```
- 提示：“此风格基于前 3 章自动学习”

### 6. 导入流程
- 点击“导入小说” → 弹窗：
   - 上传文件（支持 .txt / .docx）
   - 输入标题（必填）
   - 可选填写大纲（文本域）
- 提交后显示进度条：“正在分析小说结构...”
- 完成后自动跳转至新小说工作台

---

## 三、V2 前端功能需求（伏笔增强）

> 在 V1 基础上新增以下功能：

### 1. 伏笔管理面板（/plot-hooks）
- **列表视图**：
  | 伏笔描述 | 状态 | 可展开区间 | 操作 |
  |--------|------|----------|------|
  | “怀表停在3:15” | 待触发 | 第 4–10 章 | [标记已回收] |
  | “神秘信件” | 已触发 | 第 7 章 | — |
- **操作**：
   - “+ 添加伏笔” 按钮（弹出表单：描述 + min/max 章节）
   - 手动标记伏笔为“已回收”

### 2. 章节编辑器增强
- **伏笔提示条**（位于编辑器上方）：
  > 💡 本章可展开伏笔：**“怀表停在3:15”**（来自第 2 章）
- 若用户未处理，生成下一章时 AI 会自动延续

### 3. 大纲编辑器（/outline）
- 支持分章节大纲编写（树形结构）：
  ```
  第 1–3 章：主角抵达小镇，发现异常
    → 伏笔：井底回声
  第 4–6 章：调查失踪案
    → 关键转折：发现日记
  ```
- 大纲变更后，系统提示：“是否同步更新伏笔调度？”

### 4. 角色面板升级
- 允许 **手动编辑** 角色信念与阶段
- 显示角色出场热力图（章节分布）

### 5. 生成按钮增强
- “AI 续写” 按钮下拉菜单：
   - “普通续写”
   - “强制回收伏笔：XXX”
   - “插入高张力场景”

---

## 四、UI/UX 设计原则

| 原则 | 说明 |
|------|------|
| **沉浸式写作** | 编辑器全屏模式，隐藏非必要元素 |
| **AI 透明化** | 所有 AI 生成内容明确标注，可一键对比原文 |
| **零打断** | 自动保存，无“是否保存”弹窗 |
| **上下文可见** | 左侧边栏始终显示角色/伏笔状态 |
| **移动端友好** | 响应式布局，但优先桌面体验 |

---

## 五、V1 与 V2 功能对比表

| 功能 | V1 | V2 |
|------|----|----|
| 创建/导入小说 | ✅ | ✅ |
| 章节 AI 生成 | ✅ | ✅ |
| 风格建模（只读） | ✅ | ✅ |
| 角色管理（只读） | ✅ | ✅（可编辑） |
| 伏笔管理 | ❌ | ✅ |
| 大纲编辑 | ❌ | ✅ |
| 节奏提示 | ✅（基础） | ✅（增强） |
| 手动干预生成 | “重写本章” | +“强制回收伏笔”等 |

---

## 六、下一步建议

1. **V1 优先开发**：
   - 实现 `/dashboard` + `/novel/:id` + 章节编辑器
   - 集成富文本 + AI 生成按钮
   - 完成导入流程

2. **Mock 数据先行**：
   - 用假数据模拟角色、风格、章节
   - 确保 UI 流程顺畅后再对接后端 API

3. **关键交互验证**：
   - “AI 生成 → 用户修改 → 保存” 是否流畅？
   - 导入大文件（10万字）是否卡顿？

---

此前端方案确保 **V1 可快速上线验证核心价值**，V2 在用户反馈基础上增强专业创作能力。如需高保真原型或组件细节，可进一步细化。